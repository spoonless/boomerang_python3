une très bonne portabilité du code quel que soit le système dexploitation
sur lequel sexécute le programme pour peu que le développeur soit attentif
passant en paramètre le chemin de ce fichier le descripteur de fichier va
nous permettre de réaliser les opérations de lecture et/ou décriture dans le
géré par le système dexploitation lorsque les opérations sur le fichier sont
terminées il faut fermer le descripteur de fichier à laide de la méthode
jamais fermer les descripteurs de fichiers le système dexploitation peut
    # faire des opérations sur le contenu du fichier
le anywith <with>
        # faire des opérations sur le contenu du fichier
            # faire des opérations sur le contenu des fichiers
lire le contenu dun fichier
par défaut la fonction funcopen permet de lire le contenu dun
supposons que nous ayons un fichier nommé filedialoguestxt dans le répertoire
    caption le fichier dialoguestxt
pour obtenir le contenu du fichier dans une chaîne de caractères 
pour obtenir le contenu du fichier sous la forme dun tableau de chaînes de
    lorsquon lit un fichier texte chaque ligne inclue le caractère de retour
    à la ligne présent dans le fichier pour le supprimer on peut utiliser
lorsquon ouvre un fichier il faut préciser le mode douverture qui dépend
du type du fichier et des opérations que lon souhaite réaliser le mode est
avec le paramètre mode
    "w" "ouverture en écriture efface le contenu précédent"
    "x" "ouverture uniquement pour création louverture échoue si le fichier existe déjà"
si le paramètre encoding nest pas spécifié alors python utilise
un encodage qui est dépendant du système qui exécute le code ce qui
le mode texte entraîne également une conversion des caractères de
le mode binaire b est un mode qui permet daccéder directement au
    il faut se rappeler que le mode douverture en écriture w remplace
    intégralement le contenu du fichier pour ajouter à la fin du fichier il
    faut ouvrir le fichier en mode ajout a sous peine de perdre tout
    le contenu précédemment sauvé
dexploitation suivent le même principe il existe des différences majeures
dorganisation le système ms-windows utilise un système de fichiers multi-têtes
de fichiers ms-windows utilise le caractère \ pour séparer les composants
tandis que les systèmes \nix et macos utilisent le caractère /
enfin il faut se souvenir que le système de fichiers de ms-windows nest pas sensible
le module ospath
le module modospath fournit des fonctions élémentaires pour nous aider à
    cette fonction permet de créer un chemin en utilisant le séparateur approprié
    pour le système
    cette fonction retourne le chemin absolu
        # si le répertoire de travail est /home/david/documents
le module pathlib
le module modpathlib est un module de haut-niveau qui permet à la fois
de manipuler un chemin mais également dinteragir avec le fichier ou le répertoire
désigné par ce chemin cest un module tout-en-un qui facilite grandement le
qui accepte les mêmes paramètres que la fonction funcopen sauf le chemin
qui ouvre le fichier en mode texte lit lintégralité du fichier et referme le fichier 
division mais comme le séparateur universel de chemin de fichiers 
les répertoires soit avec les modules modos et modshutil soit avec le module modpathlib
en python ils proposent surtout des fonctions alors que le module modpathlib
connaître le répertoire de travail
le répertoire de travail correspond au répertoire courant au moment du lancement
    caption avec le module modos
    caption avec le module modpathlib
connaître le répertoire de lutilisateur
    caption avec le module modos
    caption avec le module modpathlib
    caption avec le module modshutil
    caption avec le module modos
    caption avec le module modpathlib
    caption avec le module modos
    caption avec le module modpathlib
    caption avec le module modos
    caption avec le module modpathlib
    caption avec le module modospath
    caption avec le module modpathlib
lister le contenu dun répertoire
    caption avec le module modos
    caption avec le module modpathlib
le module modglob permet deffectuer une recherche dans larborescence
de fichiers on peut utiliser le caractère  pour représenter nimporte
    caption avec le module modglob
    caption avec le module modglob
    caption avec le module modpathlib
le fichier csv comma separated values est un format texte très simple pour
stocker des tables de données le module modcsv offre des méthodes pour lire
    caption le fichier filmographiecsv
